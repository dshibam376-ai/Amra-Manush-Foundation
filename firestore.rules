rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /gallery_posts/{post} {
      allow read: if true;
      allow create: if true; // In a real app we'd check auth, but keeping it simple for now as requested
      allow update, delete: if true;
    }
  }
}
